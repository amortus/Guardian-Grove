# Guardian Grove Server - Production Environment
# Copy these variables into Railway (or chosen host) environment settings.

# Database - Railway PostgreSQL Connection String
# Get from: Railway Postgres service → Variables tab → DATABASE_URL
DATABASE_URL=postgresql://postgres:baBBeJvjWnKrwUCoZRCodaRfHloFDfpB@postgres.railway.internal:5432/railway

# JWT Secret - GENERATE A STRONG RANDOM KEY!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_STRING_IN_PRODUCTION

# Google OAuth (Optional - leave empty if not using)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://guardian-grove.vercel.app/api/auth/google/callback

# Server Configuration
PORT=3000
NODE_ENV=production

# Frontend URL (for CORS) - Set to your Guardian Grove Vercel URL
FRONTEND_URL=https://guardian-grove.vercel.app

# IMPORTANT NOTES:
# 1. Add these variables to Railway (backend service)
# 2. DATABASE_URL must be from Railway Postgres service (separate from Beast Keepers)
# 3. JWT_SECRET must be different from development
# 4. FRONTEND_URL must match your actual Vercel deployment URL
# 5. Run migrations on production database before deploying
# 6. This database is isolated from Beast Keepers - no data sharing

