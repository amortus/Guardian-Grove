{
  "name": "guardian-grove-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client & npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "build:client": "npm run build --workspace=client",
    "build:server": "npm run build --workspace=server"
  },
  "description": "Guardian Grove - Cooperative guardian training adventure",
  "repository": {
    "type": "git",
    "url": "https://github.com/amortus/Guardian-Grove"
  },
  "author": "Alysson (amortus)",
  "license": "MIT"
}

